---
name: railway-deploy
description: Deploy or redeploy the FastAPI backend on Railway. Use when asked to deploy, redeploy, or push changes to staging/production.
---

# Railway Deploy

Deploy the FastAPI backend to Railway.

## Usage

```bash
scripts/run.sh [staging|production]
```

Default: deploys to currently linked environment.
