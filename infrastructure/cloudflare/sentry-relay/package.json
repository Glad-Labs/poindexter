{
  "name": "sentry-relay",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240924.0",
    "esbuild": "^0.28.1",
    "vitest": "^3.2.6",
    "wrangler": "^4.102.0"
  },
  "overrides": {
    "esbuild": "$esbuild"
  }
}
