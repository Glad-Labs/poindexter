"""Golden-set bootstrappers — build versioned test sets from production data.

No dummy data: each capability's golden set is mined from real rows
(posts, embeddings, podcast audio). Wave 1 ships the reranker set.
"""
