"""Operator benchmark harnesses (one-off, run manually).

The cost-tier eval harness (``eval_cost_tiers.py``, #530) was retired with the
cost_tier removal (PR #1907). Add new one-off benchmark scripts here; the
energy-measurement helpers in ``services/energy_bench.py`` remain available.
"""
