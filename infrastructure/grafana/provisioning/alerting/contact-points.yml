# Grafana Contact Points
# Configure alert destinations via the Grafana UI after first boot.
# Tokens are stored in app_settings, not env vars — Grafana can't
# read them from provisioning files without causing crash loops
# when the env vars are empty.
apiVersion: 1

contactPoints: []
