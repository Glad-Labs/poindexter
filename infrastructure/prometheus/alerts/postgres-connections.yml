# Postgres connection-pool saturation alerts (GH-92).
#
# PoindexterPostgresConnectionsHigh and PoindexterPostgresConnectionsCritical
# have been moved to prometheus_rule_builder.py DEFAULT_RULES so operators
# can tune the 0.80/0.95 ratio thresholds via app_settings
# (prometheus.threshold.postgres_connection_warning_ratio /
#  prometheus.threshold.postgres_connection_critical_ratio).
#
# This file is kept as a placeholder so the prometheus.yml rule_files glob
# doesn't need updating and the GH-92 context is preserved.
