# Auto-generated files — prettier must NOT reformat these.
# They are produced verbatim by tooling (release-please, the app-settings
# doc generator, npm). Letting the pre-commit hook's `prettier --write`
# touch them produces large spurious diffs that conflict with the next
# generator run (observed 2026-05-30 when a merge restaged them: a 1500-line
# reformat of app-settings.md landed in a PR that only meant to change code).
CHANGELOG.md
docs/reference/app-settings.md
docs/reference/services.md
package-lock.json
.release-please-manifest.json

# CLAUDE.md — dense prose with inline-code/glob asterisks (e.g. `stage.*`) that
# prettier mis-pairs as emphasis and corrupts (observed 2026-06-21: `content.*`
# → `content._`, collapsed backtick spacing). It's also stats-synced by
# .github/workflows/sync-claude-md.yml, which bypasses this hook.
CLAUDE.md

# Vendored third-party runtime (React + Babel-standalone) for the operator
# console — pinned, minified, checksum-locked. prettier un-minified these on
# first commit (babel 2.98MB → 4.8MB); never reformat them. eslint already
# skips them via **/*.min.js in eslint.config.js.
src/cofounder_agent/console/js/vendor/
