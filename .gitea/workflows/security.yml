# Gitea Actions mirror of .github/workflows/security.yml. Runs the same
# gitleaks / Trivy / SBOM+grype gates on the private monorepo so breakage
# is caught before the mirror to GitHub. No SARIF upload here — Gitea has
# no code-scanning tab — results stay in the job logs + artifacts.
name: Security

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '17 6 * * 1'

jobs:
  gitleaks:
    name: gitleaks — secret scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Install gitleaks
        run: |
          curl -sSfL -o gitleaks.tar.gz \
            https://github.com/gitleaks/gitleaks/releases/download/v8.30.1/gitleaks_8.30.1_linux_x64.tar.gz
          tar -xzf gitleaks.tar.gz gitleaks
          chmod +x gitleaks
          ./gitleaks version
      - name: Run gitleaks (baselined — only NEW findings fail)
        run: |
          ./gitleaks detect \
            --config=.gitleaks.toml \
            --baseline-path=.gitleaks-baseline.json \
            --source=. \
            --verbose \
            --redact \
            --exit-code=1

  trivy-fs:
    name: Trivy — filesystem vuln scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Trivy
        # The upstream install.sh piped through /usr/local/bin failed
        # silently on the Gitea runner ("Failure - Main Install Trivy" with no
        # stderr). Fetch the release tarball directly, same pattern as the
        # gitleaks install below — zero shell wrapper, zero privileged paths.
        # Bumped to v0.69.3 to match .github/workflows/security.yml.
        run: |
          curl -sSfL -o trivy.tar.gz \
            https://github.com/aquasecurity/trivy/releases/download/v0.69.3/trivy_0.69.3_Linux-64bit.tar.gz
          tar -xzf trivy.tar.gz trivy
          chmod +x trivy
          ./trivy --version
      - name: Scan filesystem (fail on HIGH/CRITICAL)
        run: |
          ./trivy fs . \
            --severity HIGH,CRITICAL \
            --ignore-unfixed \
            --exit-code 1 \
            --format table \
            --skip-dirs '**/node_modules,**/.venv,**/__pycache__,**/.next,**/.vercel,**/dist,**/build,**/.pytest_cache,**/.claude'
      - name: Scan Dockerfiles / compose for misconfig
        run: |
          ./trivy config . \
            --severity HIGH,CRITICAL \
            --exit-code 1 \
            --format table \
            --skip-dirs '**/node_modules,**/.venv,**/__pycache__,**/.next,**/.vercel,**/dist,**/build,**/.pytest_cache,**/.claude'

  sbom-grype:
    name: SBOM + grype scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install syft + grype
        run: |
          curl -sSfL https://raw.githubusercontent.com/anchore/syft/main/install.sh \
            | sh -s -- -b /usr/local/bin v1.18.0
          curl -sSfL https://raw.githubusercontent.com/anchore/grype/main/install.sh \
            | sh -s -- -b /usr/local/bin v0.85.0
          syft version
          grype version
      - name: Generate SBOM (CycloneDX JSON)
        run: syft packages dir:. -o cyclonedx-json=sbom.cdx.json
      - name: Scan SBOM with grype (fail on HIGH/CRITICAL)
        run: grype sbom:sbom.cdx.json --fail-on high --only-fixed
      # Inline the SBOM summary into the job logs. Gitea Actions'
      # actions/upload-artifact@v3 path is broken on this runner: Gitea
      # advertises the artifact endpoint as ROOT_URL (http://localhost:3001),
      # but the act-spawned job container runs in the docker network where
      # 127.0.0.1:3001 is unreachable, so the upload retries 5x then fails
      # with `ECONNREFUSED 127.0.0.1:3001` (Glad-Labs/poindexter#131).
      # LOCAL_ROOT_URL doesn't redirect the artifact URL — Gitea uses
      # setting.AppURL (ROOT_URL) directly. Until act_runner / Gitea grow
      # a knob to override the artifact endpoint, the SBOM lives in the
      # logs. GitHub's .github/security.yml still uploads to real GitHub
      # Actions. Run `syft packages dir:. -o cyclonedx-json` locally to
      # reproduce the SBOM offline.
      - name: Print SBOM summary (artifact upload disabled — see comment)
        if: always()
        run: |
          echo "=== SBOM (CycloneDX JSON) — first 200 lines ==="
          if [ -f sbom.cdx.json ]; then
            head -200 sbom.cdx.json
            echo ""
            echo "=== SBOM stats ==="
            echo "Total bytes: $(wc -c < sbom.cdx.json)"
            echo "Component count: $(grep -c '"bom-ref"' sbom.cdx.json || echo 'unknown')"
          else
            echo "sbom.cdx.json not generated (earlier step failed)"
          fi
