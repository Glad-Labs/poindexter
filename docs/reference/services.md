# Poindexter Services Reference

> **Auto-generated by `scripts/regen-services-doc.py` — do not edit by hand.**  
> Run `python scripts/regen-services-doc.py` to refresh. CI (`.github/workflows/regen-services-doc.yml`) fails any PR where this file is out of date with the `services/` + `modules/content/` trees.
>
> 396 files across 25 packages. Each summary is the file's module-docstring first line — to improve an entry, improve that file's docstring. `services/migrations/` is excluded (schema deltas; see [migrations.md](../operations/migrations.md)).

A catalog of every service, atom, and stage in `src/cofounder_agent/services/` and `src/cofounder_agent/modules/content/`. Use it to find "what is responsible for X" without reading source. For the load-bearing subset on the critical execution path, see [CLAUDE.md's "Key services" table](../../CLAUDE.md).

---

## Table of contents

- [`services/ (top-level)`](#services-top-level) (161 files)
- [`services/audio_gen_providers/`](#servicesaudiogenproviders) (2 files)
- [`services/auth/`](#servicesauth) (4 files)
- [`services/caption_providers/`](#servicescaptionproviders) (3 files)
- [`services/data_fabric/`](#servicesdatafabric) (6 files)
- [`services/flows/`](#servicesflows) (2 files)
- [`services/image_providers/`](#servicesimageproviders) (6 files)
- [`services/integrations/`](#servicesintegrations) (9 files)
- [`services/integrations/handlers/`](#servicesintegrationshandlers) (17 files)
- [`services/jobs/`](#servicesjobs) (53 files)
- [`services/llm_providers/`](#servicesllmproviders) (7 files)
- [`services/media_compositors/`](#servicesmediacompositors) (2 files)
- [`services/pipeline_templates/`](#servicespipelinetemplates) (1 file)
- [`services/publish_adapters/`](#servicespublishadapters) (2 files)
- [`services/seo/`](#servicesseo) (2 files)
- [`services/social_adapters/`](#servicessocialadapters) (2 files)
- [`services/taps/`](#servicestaps) (11 files)
- [`services/topic_sources/`](#servicestopicsources) (10 files)
- [`services/triage/`](#servicestriage) (3 files)
- [`services/tts_providers/`](#servicesttsproviders) (2 files)
- [`services/video_providers/`](#servicesvideoproviders) (3 files)
- [`services/video_renderers/`](#servicesvideorenderers) (3 files)
- [`modules/content/`](#modulescontent) (13 files)
- [`modules/content/atoms/`](#modulescontentatoms) (54 files)
- [`modules/content/stages/`](#modulescontentstages) (18 files)

## services/ (top-level)

| File | Summary |
| --- | --- |
| `__init__.py` | Services module for AI content generation and management. |
| `admin_db.py` | Admin Database Module |
| `agent_permissions.py` | ``services.agent_permissions`` — permission gate for MCP write operations. |
| `alt_text.py` | Alt-text sanitation and budget-aware summarization. |
| `approval_service.py` | HITL approval-gate service module — single source of truth (#145). |
| `atom_registry.py` | Atom registry — discovery + cache of all ``ATOM_META`` declarations. |
| `atom_runs.py` | ``atom_runs`` — per-atom run + outcome capture for composed pipelines. |
| `audio_gen_service.py` | Audio-generation dispatcher — opt-in entry point for video + podcast. |
| `audit_log.py` | Pipeline Audit Log Service |
| `bootstrap.py` | Bootstrap helpers for the SiteConfig constructor-DI migration. |
| `bootstrap_defaults.py` | Centralized localhost / fallback URLs used across services/. |
| `brain_knowledge_read.py` | ``services.brain_knowledge_read`` — read helper for the MCP brain-knowledge tool. |
| `canonical_blog_spec.py` | The canonical_blog pipeline as a static graph_def spec (atom-cutover #355). |
| `capability_outcomes.py` | ``capability_outcomes`` — outcome feedback loop writer. |
| `category_resolver.py` | Resolve the right ``categories.id`` UUID for a post about a given topic. |
| `citation_verifier.py` | Citation verifier — Phase 6 / GH#54. |
| `cloudinary_upload_service.py` | Cloudinary Upload Service — uploads generated media to Cloudinary. |
| `container.py` | Service Container for Glad Labs AI Co-Founder |
| `container_registry.py` | Process-wide accessor for the built :class:`AppContainer`. |
| `content_db.py` | Content Database Module |
| `content_revisions_logger.py` | Content revisions logger — write-through helper for the content_revisions |
| `content_router_service.py` | Unified Content Router Service — LangGraph TemplateRunner dispatcher. |
| `content_task_store.py` | ContentTaskStore — CRUD adapter over DatabaseService for content_tasks rows. |
| `cost_aggregation_service.py` | Cost Aggregation Service |
| `cost_guard.py` | Cost Guard — pre-call budget enforcement for LLM provider plugins. |
| `cost_ledger.py` | Single read seam for cost_logs spend — splits the API and electricity axes. |
| `cost_lookup.py` | Per-token cost lookup, sourced from LiteLLM (#199 Phase 1). |
| `database_mixin.py` | Database Service Base Mixin |
| `database_service.py` | PostgreSQL Database Service Coordinator |
| `decision_service.py` | Decision Service — standard interface for ML decision logging. |
| `declarative_config_service.py` | Generic CRUD over the declarative data-plane config tables (#1522). |
| `decorators.py` | Performance Monitoring Decorators |
| `deepeval_rails.py` | DeepEval integration as a parallel content reviewer (#197 / #329). |
| `default_author.py` | Get or create the default 'Poindexter AI' author row. |
| `devto_service.py` | Dev.to Cross-Posting Service |
| `di_wiring.py` | DI-wiring helper for the module-level ``site_config`` singletons. |
| `doctor.py` | `poindexter doctor` — unified health check-graph (#527, v1 deterministic). |
| `embedding_service.py` | Embedding Service |
| `embeddings_db.py` | Embeddings Database Module |
| `energy_bench.py` | Energy-measurement helpers for the cost/energy eval harness (#530). |
| `enhanced_status_change_service.py` | Enhanced status change validation and logging service. |
| `error_handler.py` | Domain Error Classes and Error Handling |
| `excerpt_generator.py` | Excerpt generator — produces 1-2 sentence article summaries for index |
| `experiment_admin.py` | ``services.experiment_admin`` — operator CRUD for the Phase 1 lab harness. |
| `experiment_runner.py` | ``services.experiment_runner`` — variant selection for the Phase 1 lab. |
| `findings_read.py` | Read + summarize quality-probe findings for the operator console. |
| `firefighter_service.py` | Firefighter ops LLM service — diagnosis-only enrichment for alerts. |
| `gate_machinery.py` | Shared HITL approval-gate machinery (Glad-Labs/poindexter#622). |
| `gpu_scheduler.py` | GPU Scheduler — serializes access to the shared GPU across the stack's own |
| `grafana_alert_builder.py` | Render Grafana SQL alert rules from ``app_settings`` (DB-first). |
| `guardrails_rails.py` | Native QA rails for brand-fabrication + competitor-mention screening |
| `http_client.py` | Shared httpx.AsyncClient — lifespan-bound process-wide connection pool. |
| `image_captioner.py` | Vision-based alt-text captioner — describes the ACTUAL image. |
| `image_decision_agent.py` | Image Decision Agent — LLM-powered reasoning for image selection. |
| `image_service.py` | Unified Image Service |
| `image_style_rotation.py` | In-memory style-pick dedup tracker (#181). |
| `internal_rag_source.py` | Generate topic candidates from the internal embedded corpus. |
| `jwt_blocklist_service.py` | JWT Blocklist Service — server-side token invalidation. |
| `langfuse_experiments.py` | Langfuse-backed A/B experiment harness (Glad-Labs/poindexter#202). |
| `langfuse_shim.py` | ``langfuse_shim`` — single import surface for Langfuse tracing. |
| `llm_text.py` | ``llm_text`` — local-LLM plain-text chat helper, shared by atoms. |
| `logger_config.py` | Centralized Logging Configuration for Glad Labs AI Co-Founder |
| `media_approval_service.py` | Media Approval Service — per-medium distribution gate. |
| `media_asset_recorder.py` | media_asset_recorder — single source of truth for ``media_assets`` writes. |
| `media_feed_rebuild.py` | Rebuild a media RSS feed on R2 from the worker's feed route. |
| `media_pipeline_spec.py` | The ``media_pipeline`` Stage-2 graph_def spec (epic poindexter#689). |
| `media_policy.py` | Resolve a post's media plan from its niche, callable at approval time. |
| `media_quality_service.py` | Media Quality Service — deterministic Layer 1 signals. |
| `metrics_exporter.py` | Prometheus metrics exporter — exposes pipeline state via ``/metrics``. |
| `model_preferences.py` | Parse operator / API caller model selections into ``(model, provider)``. |
| `module_runner.py` | Per-module migration runner — applies one ``Module``'s migrations. |
| `newsletter_service.py` | Newsletter Service — send emails to subscribers on post publish. |
| `niche_service.py` | CRUD service for niches + their goals + their source configs. |
| `ollama_client.py` | Ollama Client Service |
| `ollama_resilience.py` | Ollama Resilience — backwards-compat alias for the generic layer |
| `pipeline_architect.py` | ``pipeline_architect`` — local-LLM service that composes pipelines on demand. |
| `pipeline_db.py` | Pipeline Database Module — writes to the new pipeline_* tables. |
| `pipeline_experiment_hook.py` | pipeline_experiment_hook — wire the A/B experiment harness into the content pipeline. |
| `pipeline_streaming.py` | Default ``on_event`` streaming callbacks for TemplateRunner progress. |
| `pipeline_throttle.py` | Pipeline Throttle Service — shared state for the approval-queue throttle. |
| `podcast_pipeline_spec.py` | The ``podcast_pipeline`` Stage-3 graph_def spec (epic poindexter#689). |
| `podcast_service.py` | Podcast Service — Text-to-Speech audio generation for published blog posts. |
| `post_pipeline_actions.py` | Post-pipeline side-effects shared by both orchestrators. |
| `posts_approval_service.py` | HITL approval-gate service for the ``posts`` table — final-publish gate. |
| `preview_screenshot.py` | Preview screenshot service |
| `profiling.py` | Continuous profiling via Pyroscope. |
| `progress_broadcaster.py` | Progress Broadcaster — stubbed out (no WebSocket clients connect). |
| `progress_service.py` | Progress Service - Real-time progress tracking for long-running tasks |
| `prometheus_rule_builder.py` | Render Prometheus alert rules from ``app_settings`` (DB-first). |
| `prompt_manager.py` | Unified Prompt Manager |
| `publish_service.py` | Shared Post Publishing Service |
| `publishing_adapters_db.py` | Read access to the ``publishing_adapters`` table (poindexter#112). |
| `qa_gates_db.py` | Read access to the ``qa_gates`` table — declarative QA chain (GH-115). |
| `qa_gates_db_writer.py` | Write-side companion to ``qa_gates_db`` — bumps run counters. |
| `quality_models.py` | Quality data models for the Unified Quality Assessment Service. |
| `quality_scorers.py` | Quality Scoring Functions |
| `r2_upload_service.py` | Object Store Upload Service — uploads media files to an S3-compatible bucket. |
| `rag_engine.py` | LlamaIndex retrieval layer over our existing pgvector embeddings (#210). |
| `ragas_eval.py` | Ragas-based RAG evaluation (#205). |
| `redis_cache.py` | Redis Caching Service for Query Performance Optimization |
| `rejection_handlers.py` | Per-gate rejection handlers — turn operator rejections into learning signals (#148). |
| `research_context.py` | Build RAG (retrieval-augmented generation) context for a new draft. |
| `research_quality_service.py` | Research Quality Service |
| `research_service.py` | Research Service — builds factual context for content generation. |
| `retention_janitor.py` | Retention janitor — legacy background loop, now a noop executor. |
| `revalidation_service.py` | ISR revalidation service — triggers Next.js cache invalidation. |
| `router_outcome_feedback.py` | ``router_outcome_feedback`` — outcome → experiment-variant weight loop. |
| `scheduled_publisher.py` | Scheduled Post Publisher |
| `scheduling_service.py` | Scheduling service — bulk + single-post queue management for |
| `seed_url_fetcher.py` | Fetch a seed URL and extract title + excerpt for topic seeding (GH-42). |
| `self_consistency_rail.py` | Self-consistency rail for multi_model_qa (#196). |
| `self_review.py` | Writer self-review pass — catch cross-section contradictions before QA. |
| `sentry_integration.py` | Error Tracking Integration — GlitchTip (Sentry-API-compatible) |
| `seo_content_generator.py` | Enhanced AI Content Generator with Full SEO and Metadata Support |
| `seo_read.py` | Read + summarize the SEO-refresh opportunity pipeline for the operator console. |
| `seo_refresh_spec.py` | seo_refresh pipeline as a static graph_def (SEO Harvest Loop Phase 2, #763). |
| `settings_defaults.py` | Consolidated default values for app_settings keys (#379). |
| `settings_service.py` | Settings Service - DB-backed key-value configuration. |
| `site_config.py` | Site Configuration — DB-first, env-fallback identity and config. |
| `skill_frontmatter.py` | Single source of truth for reading SKILL.md files. |
| `skill_importer.py` | Skill importer service (poindexter#529). |
| `social_poster.py` | Social Media Auto-Posting Service |
| `static_export_service.py` | Static Export Service — push-only headless CMS output layer. |
| `sync_service.py` | Database Sync Service for Split Architecture |
| `task_failure_alerts.py` | Task-failure alert dedup + severity routing. |
| `tasks_db.py` | Tasks Database Module |
| `tasks_mcp.py` | ``services.tasks_mcp`` — read helpers for the MCP server's task tools. |
| `telegram_config.py` | Telegram configuration — single source of truth. |
| `telemetry.py` | — |
| `template_runner.py` | TemplateRunner — LangGraph-based orchestration backbone (v1 POC). |
| `template_slug_resolver.py` | Shared resolver for ``pipeline_tasks.template_slug`` at task-create time. |
| `text_utils.py` | Text utilities for LLM-generated content. |
| `title_generation.py` | Canonical title generation + web-originality check. |
| `title_originality_external.py` | External-article title originality check (GH-87). |
| `topic_batch_service.py` | Topic batch orchestrator — replaces topic_proposal_service. |
| `topic_dedup.py` | Topic deduplication — detect duplicate candidates against the live corpus. |
| `topic_dedup_guard.py` | Pre-enqueue semantic dedup guard for manually-injected topics. |
| `topic_dedup_semantic.py` | Semantic topic deduplication using sentence embeddings (#201). |
| `topic_discovery.py` | Topic Discovery — autonomous trend scraping and topic generation. |
| `topic_length.py` | Shared target-length picker (#542). |
| `topic_pool.py` | topic_pool — data access for the niche-tagged candidate pool. |
| `topic_proposal_service.py` | Topic proposal service — manual topic injection + queue-cap helpers (#146). |
| `topic_ranking.py` | Topic candidate ranking — goal vectors + hybrid embed/LLM scoring + decay rerank. |
| `tts_service.py` | Podcast TTS dispatcher — synthesizes speech via Speaches. |
| `url_scraper.py` | URL scraper — extract title + content from any URL for topic seeding (#230). |
| `url_validator.py` | URL Validator Service — Content Pipeline Hallucination Prevention |
| `users_db.py` | Users Database Module |
| `validator_config.py` | DB-driven gate for fine-grained content validators (Validators CRUD V1). |
| `video_service.py` | Video Service — generates narrated slideshow videos from published posts. |
| `voice_agent.py` | voice_agent.py — Real-time voice agent for Poindexter. |
| `voice_agent_claude_code.py` | ClaudeCodeBridgeLLMService — Pipecat LLM stage backed by `claude -p`. |
| `voice_agent_livekit.py` | voice_agent_livekit.py — LiveKit-backed voice agent for Poindexter. |
| `voice_pipecat.py` | voice_pipecat.py — Shared Pipecat voice plumbing for every LiveKit surface. |
| `voice_prompts.py` | Shared voice-agent system prompts (Emma persona + Claude-bridge TTS) with a drift-guarded inline fallback. |
| `vram_budget.py` | Deterministic single-GPU VRAM footprint math (no I/O except read_model_arch). |
| `web_research.py` | Web Research — free local web search + content extraction. |
| `webhook_delivery_service.py` | Webhook Delivery Service - delivers pipeline events to OpenClaw. |
| `websocket_manager.py` | WebSocket Manager — stubbed out (no clients connect). |
| `worker_service.py` | Worker Service — manages worker registration, heartbeat, and capability announcement. |
| `writing_style_context.py` | Load active writing-style samples for voice matching. |
| `writing_style_db.py` | Writing Style Database Module |

## services/audio_gen_providers/

| File | Summary |
| --- | --- |
| `__init__.py` | Audio-generation provider plugins (music / SFX / ambient). |
| `stable_audio_open.py` | StableAudioOpenProvider — text-to-music/SFX via Stable Audio Open 1.0. |

## services/auth/

| File | Summary |
| --- | --- |
| `__init__.py` | Authentication services for Poindexter. |
| `oauth_client.py` | Shared OAuth Client Credentials helper (Glad-Labs/poindexter#241). |
| `oauth_issuer.py` | OAuth 2.1 JWT issuance + verification (Glad-Labs/poindexter#241). |
| `oauth_provider.py` | PoindexterOAuthProvider — implements the MCP SDK's auth Protocol. |

## services/caption_providers/

| File | Summary |
| --- | --- |
| `__init__.py` | Core caption providers shipped with Poindexter. |
| `speaches.py` | SpeachesCaptionProvider — ASR captions via the Speaches faster-whisper sidecar. |
| `whisper_local.py` | WhisperLocalCaptionProvider — local whisper.cpp transcription. |

## services/data_fabric/

| File | Summary |
| --- | --- |
| `__init__.py` | DataFabric — thin async read-only query helpers for every observability store. |
| `errors.py` | DataFabric error types. |
| `loki.py` | Thin async read-only client for Loki HTTP API. |
| `prometheus.py` | Thin async read-only client for Prometheus HTTP API. |
| `pyroscope.py` | Thin async read-only client for Pyroscope HTTP API. |
| `tempo.py` | Thin async read-only client for Tempo HTTP API. |

## services/flows/

| File | Summary |
| --- | --- |
| `__init__.py` | Prefect flows for the content pipeline (Glad-Labs/poindexter#410). |
| `content_generation.py` | ``content_generation_flow`` — Prefect-orchestrated content pipeline. |

## services/image_providers/

| File | Summary |
| --- | --- |
| `__init__.py` | services.image_providers — ImageProvider plugin implementations. |
| `_sdxl_models.py` | Shared SDXL model registry + torch availability probes. |
| `ai_generation.py` | AIGenerationProvider — LLM-crafted prompt → SDXL (or other generator). |
| `flux_schnell.py` | FluxSchnellProvider — text-to-image via FLUX.1-schnell inference server. |
| `pexels.py` | PexelsProvider — search the free Pexels stock-photo API. |
| `sdxl.py` | SdxlProvider — text-to-image via the SDXL sidecar (or in-process diffusers). |

## services/integrations/

| File | Summary |
| --- | --- |
| `__init__.py` | Declarative integrations framework. |
| `operator_notify.py` | Operator notification helper — thin wrapper over ``outbound_dispatcher.deliver``. |
| `outbound_dispatcher.py` | Outbound webhook / notification dispatcher. |
| `registry.py` | Handler registry for the declarative integrations framework. |
| `retention_runner.py` | Retention runner — walk enabled policies and execute their handlers. |
| `secret_resolver.py` | Single audited path for resolving an integration row's secret. |
| `shared_context.py` | Shared process-wide references for the integrations framework. |
| `tap_runner.py` | Tap runner — walk enabled external_taps rows and invoke each handler. |
| `telegram_cli_passthrough.py` | Telegram ``/cli`` passthrough — run ``poindexter`` CLI from your phone. |

## services/integrations/handlers/

| File | Summary |
| --- | --- |
| `__init__.py` | Handler modules for each integration surface. |
| `outbound_apprise.py` | Handler: ``outbound.apprise_notify`` — generic notification delivery. |
| `outbound_telegram.py` | Telegram Bot API helpers (``sendMessage`` / ``editMessageText``). |
| `outbound_vercel_isr.py` | Handler: ``outbound.vercel_isr``. |
| `publishing_mastodon.py` | Handler: ``publishing.mastodon``. |
| `publishing_youtube.py` | Handler: ``publishing.youtube``. |
| `retention_checkpoint_prune.py` | Handler: ``retention.checkpoint_prune``. |
| `retention_downsample.py` | Handler: ``retention.downsample``. |
| `retention_summarize_to_table.py` | Handler: ``retention.summarize_to_table``. |
| `retention_ttl_prune.py` | Handler: ``retention.ttl_prune``. |
| `tap_builtin_topic_source.py` | Handler: ``tap.builtin_topic_source`` (b1 rewrite). |
| `tap_corsair_csv.py` | Handler: ``tap.corsair_csv`` — ingest Corsair iCUE LINK CSV exports. |
| `tap_external_metrics_writer.py` | Handler: ``tap.external_metrics_writer`` — Singer-record consumer |
| `tap_singer_subprocess.py` | Handler: ``tap.singer_subprocess`` — run a Singer-spec tap binary |
| `webhook_alertmanager.py` | Handler: ``webhook.alertmanager_dispatch``. |
| `webhook_revenue.py` | Handler: ``webhook.revenue_event_writer``. |
| `webhook_subscriber.py` | Handler: ``webhook.subscriber_event_writer``. |

## services/jobs/

| File | Summary |
| --- | --- |
| `__init__.py` | Job implementations — scheduled maintenance plugins. |
| `analyze_topic_gaps.py` | AnalyzeTopicGapsJob — flag empty / low-coverage / stale categories. |
| `audit_published_quality.py` | AuditPublishedQualityJob — periodic quality re-check of published posts. |
| `backfill_post_performance_gsc.py` | BackfillPostPerformanceGscJob — retro-populate google_* columns on existing snapshots. |
| `check_memory_staleness.py` | CheckMemoryStalenessJob — alert when a pgvector writer goes silent. |
| `check_published_links.py` | CheckPublishedLinksJob — sample published posts for broken URLs. |
| `collapse_old_embeddings.py` | CollapseOldEmbeddingsJob — cap pgvector growth via cluster+summary (GH-81). |
| `crosspost_to_devto.py` | CrosspostToDevtoJob — syndicate published posts to Dev.to. |
| `db_backup.py` | DbBackupJob — invoke ``scripts/db-backup-local.sh`` on a schedule. |
| `detect_anomalies.py` | DetectAnomaliesJob — z-score outlier detection across system metrics. |
| `detect_duplicate_posts.py` | DetectDuplicatePostsJob — flag post pairs with near-identical titles. |
| `dispatch_handles.py` | Shared YouTube external-handle capture+persist for video dispatch. |
| `dispatch_media_pipeline.py` | DispatchMediaPipelineJob — the Gate-1 → Stage-2 trigger (#689 Plan 7). |
| `dispatch_podcast_pipeline.py` | DispatchPodcastPipelineJob — the Stage-3 podcast trigger (#689 deviation). |
| `enqueue_seo_refreshes.py` | Scheduled job: auto-enqueue seo_refresh tasks from open SEO opportunities. |
| `evaluate_earned_autonomy_gate2.py` | Re-evaluate pending Gate-2 rows for earned-autonomy eligibility (#531). |
| `expire_stale_approvals.py` | ExpireStaleApprovalsJob — auto-expire tasks stuck awaiting_approval. |
| `findings_alert_router.py` | FindingsAlertRouterJob — bridge ``audit_log`` findings to ``alert_events``. |
| `findings_daily_digest.py` | FindingsDailyDigestJob — once-a-day Discord summary of findings activity. |
| `fix_broken_external_links.py` | FixBrokenExternalLinksJob — strip external links returning 404. |
| `fix_broken_internal_links.py` | FixBrokenInternalLinksJob — strip ``/posts/<slug>`` links to non-published posts. |
| `fix_missing_seo.py` | FixMissingSeoJob — auto-populate missing SEO metadata for published posts. |
| `fix_uncategorized_posts.py` | FixUncategorizedPostsJob — assign a default category to NULL-category posts. |
| `flag_missing_seo.py` | FlagMissingSeoJob — surface published posts without seo_title/description. |
| `measure_seo_refresh_outcomes.py` | Scheduled job: measure the GSC outcome of an seo_refresh, N days later. |
| `media_distribute.py` | MediaDistributeJob — Stage-2 link + Gate-2-seed pass (#689 Plan 8 / 8b-2). |
| `media_reconciliation.py` | MediaReconciliationJob — DB ↔ R2 drift watchdog for podcast + video media. |
| `morning_brief.py` | MorningBriefJob — daily 7am consolidated digest of the prior 24h. |
| `niche_job_flags.py` | Per-niche scheduler-job enable/disable flags (Glad-Labs/poindexter#521). |
| `podcast_distribute.py` | PodcastDistributeJob — Stage-3 link + Gate-2-seed + RSS distribution (#689). |
| `postgres_vacuum.py` | PostgresVacuumJob — keep high-churn tables from bloating with dead tuples. |
| `probe_cloudflare_beacon.py` | ProbeCloudflareBeaconJob — active outage detector for the page-views beacon. |
| `prune_orphan_embeddings.py` | PruneOrphanEmbeddingsJob — drop embeddings whose source row is gone (#106). |
| `prune_stale_embeddings.py` | PruneStaleEmbeddingsJob — TTL-based pruning of the embeddings table (#106). |
| `reap_stale_topic_batches.py` | ReapStaleTopicBatchesJob — self-heal watchdog for wedged topic_batches. |
| `reload_site_config.py` | ReloadSiteConfigJob — periodically refresh the in-memory site_config cache. |
| `render_alertmanager_config.py` | RenderAlertmanagerConfigJob — render alertmanager.yml.tmpl with the chat_id. |
| `render_grafana_alerts.py` | RenderGrafanaAlertsJob — keep Grafana SQL alert rules in sync with app_settings. |
| `render_prometheus_rules.py` | RenderPrometheusRulesJob — keep Prometheus's rules in sync with app_settings. |
| `rollup_post_performance.py` | RollupPostPerformanceJob — aggregate page_views + external_metrics into post_performance. |
| `run_dev_diary_post.py` | RunDevDiaryPostJob — daily "what we shipped today" post generator. |
| `run_niche_topic_sweep.py` | RunNicheTopicSweepJob — periodic discovery sweep per active niche. |
| `run_retention.py` | RunRetentionJob — schedule the integrations retention_runner. |
| `run_seo_opportunity_analyzer.py` | Scheduled job: classify published posts into SEO opportunity tiers. |
| `run_taps.py` | RunTapsJob — schedule the integrations tap_runner. |
| `static_export_orphan_sweep.py` | StaticExportOrphanSweepJob — retire de-published post exports. |
| `static_export_reconciliation.py` | StaticExportReconciliationJob — DB ↔ R2 drift watchdog. |
| `sync_cloudflare_analytics.py` | SyncCloudflareAnalyticsJob — pull page-view rows from CF Analytics Engine. |
| `topic_auto_resolve.py` | TopicAutoResolveJob — auto-rank + auto-resolve open topic_batches. |
| `tune_publish_threshold.py` | TunePublishThresholdJob — auto-tune ``auto_publish_threshold`` app_setting. |
| `update_utility_rates.py` | UpdateUtilityRatesJob — refresh electricity rate + GPU TDP in app_settings. |
| `verify_published_posts.py` | VerifyPublishedPostsJob — check recently published posts return HTTP 200. |
| `youtube_payload.py` | Shared YouTube upload-payload builders (description + tags). |

## services/llm_providers/

| File | Summary |
| --- | --- |
| `__init__.py` | LLMProvider implementations — inference backend plugins. |
| `dispatcher.py` | LLMProvider dispatcher — resolve a provider instance at call time. |
| `litellm_provider.py` | LiteLLMProvider — LiteLLM-backed LLMProvider plugin. |
| `ollama_native.py` | OllamaNativeProvider — thin LLMProvider wrapper around OllamaClient. |
| `ollama_unload.py` | Ollama explicit-unload helper for VRAM-pressure pipeline transitions. |
| `openai_compat.py` | OpenAICompatProvider — one HTTP client, every OpenAI-compat backend. |
| `thinking_models.py` | Thinking-model detection. |

## services/media_compositors/

| File | Summary |
| --- | --- |
| `__init__.py` | Core media compositors shipped with Poindexter. |
| `ffmpeg_local.py` | FFmpegLocalCompositor — local ffmpeg subprocess MediaCompositor. |

## services/pipeline_templates/

| File | Summary |
| --- | --- |
| `__init__.py` | Pipeline templates — hand-coded LangGraph factories (v1 POC). |

## services/publish_adapters/

| File | Summary |
| --- | --- |
| `__init__.py` | Core publish adapters shipped with Poindexter. |
| `youtube.py` | YouTubePublishAdapter — upload finished video to YouTube. |

## services/seo/

| File | Summary |
| --- | --- |
| `__init__.py` | services.seo — read-only SEO analytics (Harvest Loop Phase 1). |
| `striking_distance.py` | Striking-distance SEO analyzer (Harvest Loop Phase 1). |

## services/social_adapters/

| File | Summary |
| --- | --- |
| `__init__.py` | Social media distribution adapters. |
| `mastodon.py` | Mastodon adapter — posts to any Mastodon/Fediverse instance. |

## services/taps/

| File | Summary |
| --- | --- |
| `__init__.py` | Tap implementations — data sources for the embedding pipeline. |
| `_chunking.py` | Shared chunking + dedup helpers for Taps. |
| `_singer.py` | SingerTap — wrap any Singer binary as a Poindexter Tap. |
| `audit.py` | AuditTap — ingest recent audit_log entries (errors + warnings). |
| `brain_decisions.py` | BrainDecisionsTap — ingest the brain_decisions audit trail. |
| `brain_knowledge.py` | BrainKnowledgeTap — ingest the brain_knowledge fact table. |
| `claude_code_sessions.py` | ClaudeCodeSessionsTap — ingest Claude Code session transcripts. |
| `memory.py` | MemoryFilesTap — ingest memory files under every Claude project scope. |
| `openclaw_sqlite.py` | OpenClawSQLiteTap — ingest OpenClaw's pre-embedded chunks into pgvector. |
| `published_posts.py` | PostsTap — ingest published posts from the ``posts`` table. |
| `runner.py` | Tap runner — iterate registered Taps and persist their Documents. |

## services/topic_sources/

| File | Summary |
| --- | --- |
| `__init__.py` | services.topic_sources — TopicSource plugin implementations. |
| `_filters.py` | Shared filters for TopicSource plugins. |
| `codebase.py` | CodebaseSource — semantic topic ideation across pgvector embeddings. |
| `dev_diary_source.py` | DevDiarySource — daily "what we shipped today" context bundle. |
| `devto.py` | DevtoSource — pull trending articles from the Dev.to / Forem API. |
| `hackernews.py` | HackerNewsSource — scrape topics from Hacker News top stories. |
| `igdb.py` | IGDBSource — pull recently-released indie games from IGDB. |
| `knowledge.py` | KnowledgeSource — offline topic ideation from brain_knowledge. |
| `runner.py` | Runner — invoke every registered TopicSource and aggregate results. |
| `web_search.py` | WebSearchSource — niche-aware topic ideation via DuckDuckGo search. |

## services/triage/

| File | Summary |
| --- | --- |
| `__init__.py` | Issue auto-triage helpers (cite-or-surface label derivation). |
| `derive_labels.py` | Content-derived issue labels — the 'cite-or-surface' core. |
| `sweep.py` | Gap analysis for the weekly issue-triage sweep. |

## services/tts_providers/

| File | Summary |
| --- | --- |
| `__init__.py` | Core TTS providers shipped with Poindexter. |
| `kokoro.py` | KokoroTTSProvider — Apache-2.0 82M-param local TTS (GH-122). |

## services/video_providers/

| File | Summary |
| --- | --- |
| `__init__.py` | Video provider plugins. |
| `ken_burns_slideshow.py` | KenBurnsSlideshowProvider — VideoProvider wrapper around the legacy |
| `wan2_1.py` | Wan21Provider — text-to-video via Wan-AI/Wan2.1-T2V-1.3B inference server. |

## services/video_renderers/

| File | Summary |
| --- | --- |
| `__init__.py` | Shot-list-driven video composition (Glad-Labs/glad-labs-stack#649). |
| `shot_list_renderer.py` | ShotListRenderer — assemble a video from a VideoShotList. |
| `shot_vision_qa.py` | Per-shot vision-QA frame scorer (video-quality Piece 2, spec §3.2). |

## modules/content/

| File | Summary |
| --- | --- |
| `__init__.py` | ``content`` business module — blog publishing workflow. |
| `ai_content_generator.py` | Unified AI Content Generator Service |
| `api.py` | Public surface of the ``content`` module. |
| `auto_publish.py` | Auto-publish helpers — quality-gated, daily-limited blog publishing. |
| `auto_publish_gate.py` | ``auto_publish_gate`` — observe-only auto-publish decision logger. |
| `content_module.py` | ``ContentModule`` — Glad Labs' blog publishing Module. |
| `content_validator.py` | Content Validator — programmatic quality gate for AI-generated content. |
| `internal_link_coherence.py` | Internal-link recommendation coherence gate. |
| `multi_model_qa.py` | Multi-Model QA — adversarial review using different LLM providers. |
| `post_edit_service.py` | Operator-grade draft editing — body + images — for awaiting_approval tasks. |
| `posts_service.py` | PostsService — single SQL owner for posts, categories, and tags. |
| `quality_service.py` | Unified Quality Assessment Service |
| `task_metadata.py` | Single source of truth for the finalize ``task_metadata`` blob. |

## modules/content/atoms/

| File | Summary |
| --- | --- |
| `__init__.py` | Atoms — atomic, single-action building blocks for pipeline templates. |
| `_citation_match.py` | Pure citation-matching core shared by the citation rail atoms. |
| `_media_render.py` | Shared render helper for the Stage-2 media render atoms (Plan 4). |
| `_narration_render.py` | Shared narration-TTS helper for the Stage-2 media render atoms (#689). |
| `_qa_persist.py` | Reject-path persistence for qa.aggregate (atom-cutover Plan 5, #355). |
| `_qa_rail_common.py` | Shared helpers for the qa.* rail atoms (atom-cutover Plan 3, #355). |
| `_seo_common.py` | Shared helpers for the SEO atoms (seo.generate_title / .generate_description |
| `approval_gate.py` | ``atoms.approval_gate`` — composable HITL pause-and-wait atom. |
| `content_check_title_originality.py` | content.check_title_originality — web-based title duplicate check. |
| `content_compile_meta.py` | content.compile_meta — pure transforms + scoring before DB write. |
| `content_evaluate_auto_publish.py` | content.evaluate_auto_publish — auto-publish gate evaluation. |
| `content_generate_draft.py` | content.generate_draft — generate the blog post body via the configured LLM. |
| `content_generate_images.py` | content.generate_images — generate images for each planned placeholder. |
| `content_generate_title.py` | content.generate_title — generate the canonical blog post title. |
| `content_inject_images.py` | content.inject_images — replace [IMAGE-N] markers with <img> tags. |
| `content_load_existing_post.py` | content.load_existing_post — hydrate PipelineState from an existing posts row. |
| `content_normalize_draft.py` | content.normalize_draft — pure text transforms on the draft body. |
| `content_persist_task.py` | content.persist_task — guarded DB write for the finalized task. |
| `content_plan_image_markers.py` | content.plan_image_markers — VRAM guard + image placeholder planning. |
| `content_reconcile_citations.py` | content.reconcile_citations — deterministic citation repair (poindexter#765). |
| `content_record_pipeline_version.py` | content.record_pipeline_version — upsert pipeline_versions after finalization. |
| `content_republish_post.py` | content.republish_post — terminal atom of the seo_refresh graph. |
| `media_load_scripts.py` | media.load_scripts — Stage-2 entry atom (epic poindexter#689). |
| `media_persist.py` | media.persist — durably store the rendered videos + record media_assets. |
| `media_qa.py` | media.qa — Stage-2 media QA atom (Plan 6, #1193). |
| `media_render_long_video.py` | media.render_long_video — Stage-2 long-form (16:9) render atom (Plan 4). |
| `media_render_narration.py` | media.render_narration — Stage-2 narration TTS atom (#689). |
| `media_render_short_video.py` | media.render_short_video — Stage-2 short-form (9:16) render atom (Plan 4). |
| `media_transcribe_narration.py` | media.transcribe_narration — Stage-2 per-lane ASR atom (#676 / #689). |
| `narrate_bundle.py` | ``narrate_bundle`` atom — bundle in, narrative paragraphs out. |
| `podcast_load_script.py` | podcast.load_script — Stage-3 entry atom (#689 deviation, ``podcast_pipeline``). |
| `podcast_persist.py` | podcast.persist — Stage-3 terminal atom (#689 deviation, ``podcast_pipeline``). |
| `podcast_render.py` | podcast.render — Stage-3 render atom (#689 deviation, ``podcast_pipeline``). |
| `qa_aggregate.py` | qa.aggregate — combine the qa.* rail reviews into the QA gate decision. |
| `qa_audio.py` | qa.audio — Stage-2 narration audio QA atom (#1193, Phase 2; dual-lane #689). |
| `qa_citation.py` | qa.citations — the dead-link / minimum-citation gate as a rail atom. |
| `qa_consistency.py` | qa.consistency — the internal self-contradiction gate as a rail atom. |
| `qa_critic.py` | qa.critic — the legacy adversarial LLM critic as a composable atom. |
| `qa_deepeval.py` | qa.deepeval — the DeepEval rail family as one composable atom. |
| `qa_programmatic.py` | qa.programmatic — the programmatic ContentValidator as a composable QA rail. |
| `qa_ragas.py` | qa.ragas — the Ragas rail as one composable atom. |
| `qa_rewrite.py` | qa.rewrite — one bounded revision pass for a critic-vetoed draft. |
| `qa_self_consistency.py` | qa.self_consistency — HalluCounter-style self-consistency rail as an atom. |
| `qa_topic_delivery.py` | qa.topic_delivery — the bait-and-switch topic-delivery gate as a rail atom. |
| `qa_unlinked_attribution.py` | qa.unlinked_attribution — advisory rail for named sources cited without links. |
| `qa_vision.py` | qa.vision — the vision/preview QA gate as one composable rail atom. |
| `qa_web_factcheck.py` | qa.web_factcheck — the web-grounded fact-check rail as a composable atom. |
| `review_with_critic.py` | ``atoms.review_with_critic`` — single-LLM critic review atom. |
| `seo_extract_keywords.py` | seo.extract_keywords — LLM-proposed, content-grounded SEO keywords. |
| `seo_generate_all_metadata.py` | seo.generate_all_metadata — single structured LLM call for all SEO metadata. |
| `seo_generate_description.py` | seo.generate_description — LLM-written meta description (150-160 chars). |
| `seo_generate_title.py` | seo.generate_title — LLM-written, SEO-optimized blog title atom. |
| `seo_optimize_metadata.py` | seo.optimize_metadata — query-aware re-optimization of an existing post's |
| `two_pass_writer.py` | ``atoms.two_pass_writer`` — internal-first draft, then conditional |

## modules/content/stages/

| File | Summary |
| --- | --- |
| `__init__.py` | Core pipeline Stages — one file per Stage, discovered via entry_points. |
| `approval_gate.py` | ApprovalGateStage — generic HITL pause-and-wait Stage (#145). |
| `caption_images.py` | CaptionImagesStage — re-caption inline + featured images with vision. |
| `capture_training_data.py` | CaptureTrainingDataStage — stage 6 of the content pipeline. |
| `finalize_task.py` | FinalizeTaskStage — stage 7 of the content pipeline. |
| `generate_content.py` | GenerateContentStage — stage 2 of the content pipeline. |
| `generate_media_scripts.py` | GenerateMediaScriptsStage — stage 4B of the content pipeline. |
| `generate_seo_metadata.py` | GenerateSeoMetadataStage — stage 4 of the content pipeline. |
| `generate_video_shot_list.py` | GenerateVideoShotListStage — director output for the post's video. |
| `quality_evaluation.py` | QualityEvaluationStage — stage 2B of the content pipeline. |
| `replace_inline_images.py` | ReplaceInlineImagesStage — stage 2C of the content pipeline. |
| `resolve_internal_link_placeholders.py` | ResolveInternalLinkPlaceholdersStage — convert ``[posts/<slug>]`` |
| `review_video_shot_list.py` | ReviewVideoShotListStage — director self-critique of the video shot list. |
| `source_featured_image.py` | SourceFeaturedImageStage — stage 3 of the content pipeline. |
| `topic_decision_gate.py` | TopicDecisionGateStage — topic-decision HITL gate (#146). |
| `url_validation.py` | UrlValidationStage — stage 2B.1 of the content pipeline. |
| `verify_task.py` | VerifyTaskStage — confirm the task row exists before the pipeline works on it. |
| `writer_self_review.py` | WriterSelfReviewStage — stage 2A.5 of the content pipeline. |

---

## Content pipeline (`canonical_blog` graph_def) — 39 nodes

Rendered in execution order from `services/canonical_blog_spec.py::CANONICAL_BLOG_GRAPH_DEF` (11 `stage.*` + 12 `content.*` + 13 `qa.*` + 1 `seo.*` + 2 `atoms.approval_gate`). `stage.*` atoms live in `modules/content/stages/`; `content.*` / `qa.*` / `seo.*` and the approval gate in `modules/content/atoms/`.

1. `verify_task` → `stage.verify_task`
2. `generate_draft` → `content.generate_draft`
3. `generate_title` → `content.generate_title`
4. `check_title_originality` → `content.check_title_originality`
5. `normalize_draft` → `content.normalize_draft`
6. `draft_gate` → `atoms.approval_gate`
7. `writer_self_review` → `stage.writer_self_review`
8. `resolve_internal_link_placeholders` → `stage.resolve_internal_link_placeholders`
9. `reconcile_citations` → `content.reconcile_citations`
10. `quality_evaluation` → `stage.quality_evaluation`
11. `url_validation` → `stage.url_validation`
12. `plan_image_markers` → `content.plan_image_markers`
13. `generate_images` → `content.generate_images`
14. `inject_images` → `content.inject_images`
15. `source_featured_image` → `stage.source_featured_image`
16. `caption_images` → `stage.caption_images`
17. `qa_programmatic` → `qa.programmatic`
18. `qa_critic` → `qa.critic`
19. `qa_deepeval` → `qa.deepeval`
20. `qa_ragas` → `qa.ragas`
21. `qa_vision` → `qa.vision`
22. `qa_topic_delivery` → `qa.topic_delivery`
23. `qa_citations` → `qa.citations`
24. `qa_unlinked_attribution` → `qa.unlinked_attribution`
25. `qa_consistency` → `qa.consistency`
26. `qa_self_consistency` → `qa.self_consistency`
27. `qa_web_factcheck` → `qa.web_factcheck`
28. `qa_aggregate` → `qa.aggregate`
29. `qa_rewrite` → `qa.rewrite`
30. `seo_all_metadata` → `seo.generate_all_metadata`
31. `generate_media_scripts` → `stage.generate_media_scripts`
32. `generate_video_shot_list` → `stage.generate_video_shot_list`
33. `review_video_shot_list` → `stage.review_video_shot_list`
34. `capture_training_data` → `stage.capture_training_data`
35. `compile_meta` → `content.compile_meta`
36. `persist_task` → `content.persist_task`
37. `record_pipeline_version` → `content.record_pipeline_version`
38. `preview_gate` → `atoms.approval_gate`
39. `evaluate_auto_publish` → `content.evaluate_auto_publish`

---

## What's NOT in this catalog (deleted / consolidated)

Referenced in older docs or git history but no longer on disk under these trees — listed so a grep doesn't send you on a wild goose chase.

- **`task_executor.py`** — Legacy polling daemon. Deleted 2026-05-16 (Prefect Stage 4, Glad-Labs/poindexter#410). Replaced by `flows/content_generation.py`; helpers moved to `integrations/operator_notify.py`, `integrations/handlers/webhook_alertmanager.py`, and `auto_publish.py`.
- **`model_router.py` / `usage_tracker.py` / `model_constants.py`** — Legacy LLM-router trio. Deleted 2026-05-08 (Phase 2). Replaced by `cost_lookup.py` + `llm_providers/dispatcher.py::resolve_tier_model`.
- **`workflow_executor.py` + `custom_workflows_service.py` + `template_execution_service.py` + `workflow_validator.py` + `phase_mapper.py` + `phase_registry.py` + `workflow_progress_service.py` + `phases/` + `schemas/custom_workflow_schemas.py` + `agents/`** — The workflow-executor chain. Deleted 2026-05-09 (~3,800 LOC). Replaced by `template_runner.py` (LangGraph TemplateRunner).
- **`experiment_service.py`** — A/B harness. Deleted 2026-05-10 (Glad-Labs/poindexter#202). Replaced by `langfuse_experiments.py` (Langfuse Datasets/Traces/Scores).
- **`plugins/stage_runner.py`** — Legacy chunked StageRunner. Deleted 2026-05-16 (Lane C Stage 4). `content_router_service.py` is now a thin TemplateRunner dispatcher.
- **`admin_database.py` / `content_database.py` / `embeddings_database.py` / `tasks_database.py` / `users_database.py` / `writing_style_database.py`** — The six modular DB files. Consolidated into `database_service.py` (+ `database_mixin.py`); callers reach typed CRUD through the single coordinator.
- **`image_generation_config.py` / `image_generation_runner.py` / `image_prompt_builder.py` / `image_selection_service.py`** — Old image stack. Restructured into the `services/image_providers/` package (sdxl / pexels / flux_schnell / ai_generation) + `image_service.py` / `image_captioner.py` / `image_decision_agent.py` + content image atoms/stages under `modules/content/`.
- **`notifications_service.py`, `paging_helpers.py`, `html_sanitizer.py`, `slugify_service.py`, `quality_checker.py`, `rag_embeddings_service.py`, `vector_similarity_search.py`, `media_script_generator.py`, `transcription_service.py`, `handle_task_status_change.py`, `stateless_decision_handler.py`, `idle_worker.py`** — Removed in the 2026-05/06 cleanup waves (no direct successor — folded into callers or made obsolete by the atom pipeline). `idle_worker.py` retired in #1171.

---

## Conventions

- **Config via DI.** Services don't import a module-level `site_config`; they receive `SiteConfig` through DI — `Depends(get_site_config_dependency)` in routes, a constructor kwarg in services, `context.get("site_config")` in pipeline atoms. See CLAUDE.md's Configuration section.
- **Async-everywhere.** Blocking work runs in `asyncio.to_thread` or a worker queue; never block the event loop.
- **Typed errors** via `error_handler.py` (the `AppError` hierarchy); routes translate them to HTTP status codes.
- **Atoms don't import each other.** `stage.*` / `content.*` / `qa.*` / `seo.*` atoms are wired into the `canonical_blog` graph_def and communicate only through `PipelineState` channels, never direct imports.
