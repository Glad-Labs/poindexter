# Gitea Actions CI — local test runner (host mode)
name: CI — Test

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        run: |
          git clone --depth 1 --branch ${{ github.ref_name }} http://gitea:3000/gladlabs/glad-labs-codebase.git workspace
          cd workspace

      - name: Install tools
        run: |
          apk add --no-cache python3 py3-pip nodejs npm

      - name: Backend tests
        run: |
          cd workspace
          pip3 install --break-system-packages --upgrade pip poetry
          poetry config virtualenvs.create false
          cd src/cofounder_agent && poetry install --no-root
          poetry run pytest tests/unit/ -q --tb=short --ignore=tests/unit/services/test_web_research.py --ignore=tests/unit/agents/test_content_agent_pexels_client.py --ignore=tests/unit/agents/test_content_agent_postgres_image_agent.py --ignore=tests/unit/routes/test_newsletter_routes.py --ignore=tests/unit/services/test_memory_client.py -x
