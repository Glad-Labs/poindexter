[project]
name = "gladlabs-mcp"
version = "1.0.0"
description = "Glad Labs MCP Server — AI content pipeline control"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
