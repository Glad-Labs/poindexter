# Runtime-rendered scrape secrets. Brain writes API keys / passwords
# here from app_settings on every cycle (see
# brain/prometheus_secret_writer.py). Never commit the actual values.
*
!.gitignore
!.gitkeep
