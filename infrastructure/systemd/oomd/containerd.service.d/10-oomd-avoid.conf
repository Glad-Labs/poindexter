# Deprioritise for systemd-oomd: killing containerd takes every container with it,
# which is strictly worse than losing the one sidecar that caused the pressure.
[Service]
ManagedOOMPreference=avoid
