{
  "name": "unsubscribe-relay",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260918.1",
    "esbuild": "^0.28.2",
    "vitest": "^5.0.1",
    "wrangler": "^4.134.0"
  }
}
